# When hosting the frontend separately (e.g. on Vercel, Netlify, Cloudflare Pages, S3):
# Set this to your deployed FastAPI backend URL (without trailing slash)
# Example: VITE_API_BASE_URL=https://trimatch-api.onrender.com

VITE_API_BASE_URL=
